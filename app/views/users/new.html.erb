<%= form_for @user do |f| -%>
  <%= f.text_field :name %>
  <%= f.password_field :password %>
  <%= f.password_field :confirm_password %>
  <%= f.submit "Submit", :disable_with => 'Submiting...' %>
<% end %>